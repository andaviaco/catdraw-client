import React, { Component } from 'react';
import ToolBar from './ToolBar';

class Draw extends Component{
  render() {
    return (
      <section>
        <ToolBar />
      </section>
    );
  }
}

export default Draw;
